#include <stdio.h>

int main() {  

    char name[] = {'O','Y','E','S','\0','T','E','R',' ','K','O','D','E','\0'};
    printf("%s",name);
    
    /*
    for(int i = 0; i < 14; i++) {
        printf("%c ", name[i]);
    }
    
    printf("\n%d", sizeof(name));
    */


    return 0;
}